
Testing
1. npm install --save-dev jest
2.Add a test script inside package.json that runs our tests: "test": "jest --verbose",  and
"jest": {
    "testEnvironment": "node"
    }
3 create testing folder, and file with name of js file you are creating with a .test.js so we know its a test file.
4.import superstart
5.Import endpoint you will be testing, make sure that before you import it however


Testing your Endpoint that connects to your db
-----
6. on you knexfile, make sure you set up a test script with a file name like authTest.js for example
7. Then run lastest update thi way: npx knex migrate:latest --env=dev


